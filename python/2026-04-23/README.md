Run the script:

```
python file-op.py
```

- Enter a directory path when prompted.
- Optionally enter a keyword to filter file names.
- Choose whether to sort files by size (descending or not).
- The script will display the file list and summary.
- You can preview a file by entering its name.
- Optionally save the summary to summary.txt.
